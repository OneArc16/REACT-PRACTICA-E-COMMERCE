import React from 'react'

export default function Hola(props) {
  // const {userInfo, userFn} = props;
  // const {nombre} = userInfo;
  return (
    <div>
      {/* <button onClick={() => props.userFn(nombre)}>Saludar</button> */}
    </div>
  )
}
